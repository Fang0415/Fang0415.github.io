// Generated from the core LinkRag README at remote HEAD 81a093aa5c4edcd8b46beb22c45d5c788a03683a.
// The project page keeps only the concise product story, core capabilities, and live demo.
export const LINKRAG_README_ZH = "人人可用的企业级 RAG 系统——把复杂文档，变成可以对话的知识。\n\n## LinkRag 是什么？\n\n`LinkRag` 是一款从0开始完全自研自建的已上线RAG项目，立意之初是为了作为Agent的数据底座而构建，提供非结构化文档的存储召回。目前已经基于传统路线实现向量RAG，构建出完整的Web服务，可作为通用知识库解决企业/个人知识问答场景。同时我们也在拓展Agent的落地场景，将文档的存与查拓展到辅助写成为长期记忆底座。\n\n本仓库是其中的 **Python RAG 服务**，也是整个系统的引擎：它把各种复杂格式的文档解析成结构化 Markdown，按语义切成可检索的知识单元，建立稠密、稀疏、BM25 三路索引；在查询时多路并行召回、融合、重排，最终基于真正检索到的内容生成有据可查的答案。整个过程通过消息队列与 Java 业务系统异步集成，业务侧只管下发任务，不必关心解析与检索的内部实现。\n\n## 核心能力\n\n- 将 PDF、Word、HTML 等复杂文档解析为结构化 Markdown。\n- 结合标题层级与语义分析进行分片，保护表格、代码、公式和图片等完整元素。\n- 并行执行稠密、稀疏和 BM25 检索，通过 RRF 融合并支持 rerank 精排。\n- 基于检索证据生成可追溯的 SSE 流式问答，没有足够依据时明确返回无答案。\n- 处理任务支持断点续跑，多租户可按用户配置不同模型。\n\n## 在线体验\n\n线上地址：[http://linkrag.cn/](http://linkrag.cn/)。上传文档、自动构建知识库，围绕内容直接提问，答案逐字流式返回，并溯源到原文片段。";

export const LINKRAG_README_EN = "An enterprise-grade RAG system for everyone — turn complex documents into knowledge you can talk to.\n\n## What is LinkRag?\n\n`LinkRag` is an enterprise-grade RAG system that lets anyone turn their own documents into a conversational knowledge base — covering the full pipeline from document ingestion, parsing, chunking, and vectorization to retrieval and answer generation.\n\nThis repository is the **Python RAG service** at its core, and the engine of the whole system: it parses documents in all kinds of complex formats into structured Markdown, splits them semantically into retrievable knowledge units, and builds three indexes — dense, sparse, and full-text. At query time it recalls across multiple paths in parallel, fuses, and reranks, finally producing grounded answers based only on what was actually retrieved. The entire process integrates asynchronously with a Java business system over a message queue — the business side only dispatches tasks and reads terminal states, without needing to know how parsing and retrieval work inside.\n\n## Core capabilities\n\n- Parse complex PDF, Word, and HTML documents into structured Markdown.\n- Use heading-aware semantic chunking while protecting tables, code, formulas, and images.\n- Run dense, sparse, and BM25 retrieval in parallel, fuse results with RRF, and optionally rerank.\n- Stream grounded SSE answers with traceable evidence and an explicit no-answer state.\n- Resume interrupted processing and resolve model capabilities from each tenant's configuration.\n\n## Live Demo\n\nLive at [http://linkrag.cn/](http://linkrag.cn/). Upload documents, build a knowledge base automatically, ask questions directly about the content, and get answers streamed token by token, traced back to the source passages.";
